import {Container, Form, Title, Subtitle} from '../../types/index';
import styled from 'styled-components';

export const ThankYouContainer = styled(Container)`
    justify-content:center;
    align-items:center;

`;

export const ThankYouForm = styled(Form)`
    justify-content:center;
    align-items:center;
    margin:0;
    text-align:center;
    top:30%;

`;

export const ThankYouTitle = styled(Title)`

`;

export const ThankYouSubtitle = styled(Subtitle)`
    padding:2rem;

`;

export const ThankYouImg = styled.img`
    width: 100%;
    height: 100%;
    object-fit: cover;

`;

export const ThankYouImgContainer = styled.picture`
    width:auto;
    height: auto;


`;